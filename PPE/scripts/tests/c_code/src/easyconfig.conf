* GLOBAL:
   FORMAT               =  "%func __ %msg"
##    FORMAT               =  "%datetime %func %msg"
   FILENAME             =  "./my.log"
   ENABLED              =  true
   TO_FILE              =  true
   TO_STANDARD_OUTPUT   =  false
   SUBSECOND_PRECISION  =  2
   PERFORMANCE_TRACKING =  true
   MAX_LOG_FILE_SIZE    =  102097152 ## 2MB - Comment starts with two hashes (##)
   LOG_FLUSH_THRESHOLD  =  100 ## Flush after every 100 logs
* DEBUG:
   FORMAT               = "%datetime{%d/%M} %func %msg"
   ENABLED              =  false
   TO_FILE              =  true
   TO_STANDARD_OUTPUT   =  false
   SUBSECOND_PRECISION  =  2
   PERFORMANCE_TRACKING =  true
   MAX_LOG_FILE_SIZE    =  102097152 ## 2MB - Comment starts with two hashes (##)
   LOG_FLUSH_THRESHOLD  =  100 ## Flush after every 100 logs
* INFO:
   FORMAT               = "INFO: %msg"
   ENABLED              =  true
   FILENAME             = "mylog.log"
   TO_FILE              =  true
   TO_STANDARD_OUTPUT   =  false
   SUBSECOND_PRECISION  =  2
   PERFORMANCE_TRACKING =  true
   MAX_LOG_FILE_SIZE    =  102097152 ## 2MB - Comment starts with two hashes (##)
   LOG_FLUSH_THRESHOLD  =  100 ## Flush after every 100 logs
* OUT:
   FORMAT               = "INFO: %msg"
   ENABLED              =  true
   FILENAME             = "mylog.log"
   TO_FILE              =  true
   TO_STANDARD_OUTPUT   =  false
   SUBSECOND_PRECISION  =  2
   PERFORMANCE_TRACKING =  true
   MAX_LOG_FILE_SIZE    =  102097152 ## 2MB - Comment starts with two hashes (##)
   LOG_FLUSH_THRESHOLD  =  100 ## Flush after every 100 logs